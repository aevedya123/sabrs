services:
  - type: web
    name: discord-link-bot
    env: python
    plan: free
    region: oregon   # you can change this if you’re elsewhere
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
